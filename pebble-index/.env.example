# Notion connection token with access to the target database — https://app.notion.com/developers/connections
NOTION_API_TOKEN=
# Database to create voice-note pages in (NOTION_ prefix is reserved on remote env)
PEBBLE_DATABASE_ID=
